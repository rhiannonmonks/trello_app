<%= form_for(@card_assignment) do |f| %>
  <% if @card_assignment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@card_assignment.errors.count, "error") %> prohibited this card_assignment from being saved:</h2>

      <ul>
      <% @card_assignment.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :card_id %><br>
    <%= f.number_field :card_id %>
  </div>
  <div class="field">
    <%= f.label :user_id %><br>
    <%= f.number_field :user_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
